{
  "name": "bookme",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "postinstall": "npm run build && cp -r frontend/dist backend/public"
  },
  "dependencies": {}
}
